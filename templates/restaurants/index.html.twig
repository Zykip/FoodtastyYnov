{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% extends 'partial/base.html.twig' %}
{% block body %}
    <div class="container">
        <h1>Profile</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
