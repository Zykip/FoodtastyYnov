{% extends 'partial/base.html.twig' %}

{% block title %}New Dishes{% endblock %}

{% block body %}
    <h1 class="d-inline-block">Create new Dishes</h1>
    <a href="{{ path('dishes_index') }}" class="btn btn-primary float-right">back to list</a>

    {{ include('dishes/_form.html.twig') }}

{% endblock %}
